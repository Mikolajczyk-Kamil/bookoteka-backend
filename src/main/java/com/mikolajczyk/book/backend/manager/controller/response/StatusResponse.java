package com.mikolajczyk.book.backend.manager.controller.response;

public enum StatusResponse {
    SUCCESS,
    FAILED,
    BOOK_NOT_FOUND,
    USER_NOT_FOUND,
    TOKEN_NOT_FOUND,
    VERIFICATION_FAILED
}
